aiogram>=3.4.0
google-genai>=0.3.0
aiohttp-socks>=0.8.4
psutil>=5.9.0

# SimpleMemProvider
simplemem>=0.1.0

# SemanticProvider
lancedb
sentence-transformers
numpy
pyarrow

# HindsightProvider (embedded server mode)
# On Windows, vendor/uvloop-stub must be installed first to satisfy uvloop dependency
vendor/uvloop-stub
hindsight-all
