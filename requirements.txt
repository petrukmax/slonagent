aiogram>=3.4.0
google-genai>=0.3.0
aiohttp-socks>=0.8.4
psutil>=5.9.0

# SimpleMemProvider
simplemem>=0.1.0

# SemanticProvider
lancedb
sentence-transformers
numpy
pyarrow

# HindsightProvider (embedded server mode)
# On Windows, stubs for Unix-only modules must be installed first
vendor/uvloop-stub
vendor/fcntl-stub
vendor/resource-stub
hindsight-all
