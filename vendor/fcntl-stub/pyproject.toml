[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fcntl-stub"
version = "1.0.0"
description = "Windows stub for fcntl (no-op file locking)"

[tool.setuptools]
py-modules = ["fcntl"]
